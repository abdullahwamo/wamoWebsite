import React from 'react';
import HomeScreenIndex from '@/components/home-page';

const Home: React.FC = () => {
  return (
    <div>
      <HomeScreenIndex />
    </div>
  );
};

export default Home;
